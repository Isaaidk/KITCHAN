diff --git a/kitchan-backend/src/kitchan/__pycache__/main.cpython-312.pyc b/kitchan-backend/src/kitchan/__pycache__/main.cpython-312.pyc
index 8fd4c99..6598d7f 100644
Binary files a/kitchan-backend/src/kitchan/__pycache__/main.cpython-312.pyc and b/kitchan-backend/src/kitchan/__pycache__/main.cpython-312.pyc differ
diff --git a/kitchan-backend/src/kitchan/modules/integraciones/core/domain/__pycache__/inter_module_ports.cpython-312.pyc b/kitchan-backend/src/kitchan/modules/integraciones/core/domain/__pycache__/inter_module_ports.cpython-312.pyc
index 6dcf062..ccab423 100644
Binary files a/kitchan-backend/src/kitchan/modules/integraciones/core/domain/__pycache__/inter_module_ports.cpython-312.pyc and b/kitchan-backend/src/kitchan/modules/integraciones/core/domain/__pycache__/inter_module_ports.cpython-312.pyc differ
diff --git a/kitchan-backend/src/kitchan/modules/integraciones/infrastructure/adapters/uber_http_adapter.py b/kitchan-backend/src/kitchan/modules/integraciones/infrastructure/adapters/uber_http_adapter.py
index 4b7101d..471e4b4 100644
--- a/kitchan-backend/src/kitchan/modules/integraciones/infrastructure/adapters/uber_http_adapter.py
+++ b/kitchan-backend/src/kitchan/modules/integraciones/infrastructure/adapters/uber_http_adapter.py
@@ -5,21 +5,32 @@ from src.kitchan.modules.integraciones.uber.domain.ports import UberApiPort
 class UberHttpAdapter(UberApiPort):
     
     async def get_order_details(self, order_id: str, access_token: str) -> dict:
-        
-        # --- 🧪 INTERCEPTOR DE PRUEBAS (Evita el 401 de Uber) ---
-        # --------------------------------------------------------
 
-        # Código real para producción
-        url = f"https://api.uber.com/v2/eats/order/{order_id}"
-        headers = {"Authorization": f"Bearer {access_token}", "Content-Type": "application/json"}
-        
-        async with httpx.AsyncClient() as client:
-            respuesta = await client.get(url, headers=headers)
-            
-        if respuesta.status_code != 200:
-            raise HTTPException(status_code=respuesta.status_code, detail=f"Error en Uber: {respuesta.text}")
-            
-        return respuesta.json()
+        url = f"{self.BASE_URL}/v1/delivery/order/{order_id}"
+
+        headers = {
+            "Authorization": f"Bearer {access_token}",
+            "Content-Type": "application/json",
+        }
+
+        print(f"🔎 [UBER API] GET ORDER -> {url}")
+
+        async with httpx.AsyncClient(timeout=30) as client:
+            response = await client.get(
+                url,
+                headers=headers
+            )
+
+        print(f"📡 [UBER API] GET ORDER STATUS -> {response.status_code}")
+        print(f"📦 [UBER API] GET ORDER RESPONSE -> {response.text}")
+
+        if response.status_code != 200:
+            raise HTTPException(
+                status_code=response.status_code,
+                detail=f"Error en Uber: {response.text}"
+            )
+
+        return response.json()
 
     async def accept_order(self, order_id: str, access_token: str, reason: str = "Accepted") -> bool:
         url = f"https://api.uber.com/v2/eats/order/{order_id}/accept_pos_order"
@@ -57,4 +68,88 @@ class UberHttpAdapter(UberApiPort):
             
         return True
 
-    
\ No newline at end of file
+    # ============================================================
+# CANCELAR ORDEN
+# ============================================================
+
+async def cancel_order(
+    self,
+    order_id: str,
+    access_token: str,
+    reason: str,
+    details: str | None = None
+) -> bool:
+
+    url = (
+        f"{self.BASE_URL}"
+        f"/v1/eats/orders/{order_id}/cancel"
+    )
+
+    headers = {
+        "Authorization": f"Bearer {access_token}",
+        "Content-Type": "application/json",
+    }
+
+    payload = {
+        "reason": reason
+    }
+
+    if details:
+        payload["details"] = details
+
+    print(
+        f"📤 [UBER API] POST {url}"
+    )
+
+    print(
+        f"📦 [UBER API] Payload: {payload}"
+    )
+
+    async with httpx.AsyncClient() as client:
+
+        response = await client.post(
+            url,
+            headers=headers,
+            json=payload,
+            timeout=30
+        )
+
+    print(
+        f"📡 [UBER API] CANCEL ORDER -> "
+        f"{response.status_code}"
+    )
+
+    if response.status_code not in (200, 204):
+
+        print(
+            f"❌ [UBER API] Error cancelando orden "
+            f"{order_id}"
+        )
+
+        print(
+            f"Status: {response.status_code}"
+        )
+
+        print(
+            f"Response: {response.text}"
+        )
+
+        raise HTTPException(
+            status_code=response.status_code,
+            detail={
+                "error": "UBER_CANCEL_FAILED",
+                "mensaje": (
+                    "Uber rechazó la cancelación "
+                    "de la orden"
+                ),
+                "uber_status": response.status_code,
+                "uber_response": response.text
+            }
+        )
+
+    print(
+        f"✅ [UBER API] Orden {order_id} "
+        f"cancelada correctamente"
+    )
+
+    return True
\ No newline at end of file
diff --git a/kitchan-backend/src/kitchan/modules/integraciones/uber/application/__pycache__/order_use_cases.cpython-312.pyc b/kitchan-backend/src/kitchan/modules/integraciones/uber/application/__pycache__/order_use_cases.cpython-312.pyc
index 5aac451..586f36c 100644
Binary files a/kitchan-backend/src/kitchan/modules/integraciones/uber/application/__pycache__/order_use_cases.cpython-312.pyc and b/kitchan-backend/src/kitchan/modules/integraciones/uber/application/__pycache__/order_use_cases.cpython-312.pyc differ
diff --git a/kitchan-backend/src/kitchan/modules/integraciones/uber/application/__pycache__/webhook_use_cases.cpython-312.pyc b/kitchan-backend/src/kitchan/modules/integraciones/uber/application/__pycache__/webhook_use_cases.cpython-312.pyc
index 8a4a6dc..399463d 100644
Binary files a/kitchan-backend/src/kitchan/modules/integraciones/uber/application/__pycache__/webhook_use_cases.cpython-312.pyc and b/kitchan-backend/src/kitchan/modules/integraciones/uber/application/__pycache__/webhook_use_cases.cpython-312.pyc differ
diff --git a/kitchan-backend/src/kitchan/modules/integraciones/uber/application/order_use_cases.py b/kitchan-backend/src/kitchan/modules/integraciones/uber/application/order_use_cases.py
index 6fce9f8..4d360f4 100644
--- a/kitchan-backend/src/kitchan/modules/integraciones/uber/application/order_use_cases.py
+++ b/kitchan-backend/src/kitchan/modules/integraciones/uber/application/order_use_cases.py
@@ -38,20 +38,34 @@ class UberOrderUseCase:
             estado="PENDING"
         )
 
-    async def accept_order_in_uber(self, order_id: str, restaurante_id: str) -> bool:
-        """Flujo cuando el usuario presiona 'Aceptar' en el frontend de Kitchan."""
-        # 1. Recuperamos el token de Redis
-        token = await self.token_cache.get_token(restaurante_id)
+    async def accept_order_in_uber(
+        self,
+        order_id: str,
+        restaurante_id: str
+    ) -> bool:
+
+        # Para operaciones de pedidos Uber exige
+        # Client Credentials + eats.order
+        token = await self.token_cache.get_app_token(
+            restaurante_id
+        )
+
         if not token:
-            raise ValueError("Token de Uber no encontrado o expirado. Vuelva a conectar la tienda.")
+            raise ValueError(
+                "App Token de Uber no encontrado o expirado. "
+                "Ejecute /app-token para generarlo."
+            )
+
+        await self.uber_api.accept_order(
+            order_id,
+            token
+        )
+
+        print(
+            f"✅ [NEGOCIO] Orden {order_id} "
+            f"aceptada exitosamente en Uber."
+        )
 
-        # 2. Llamamos a Uber para aceptar la orden
-        await self.uber_api.accept_order(order_id, token)
-        
-        print(f"✅ [NEGOCIO] Orden {order_id} aceptada exitosamente en Uber.")
-        
-        # TODO: Aquí llamarías al caso de uso interno de `pedidos` para cambiar 
-        # el estado de la orden en nuestra base de datos SQL a "EN_PREPARACION".
         return True
 
     async def process_notification(self, payload: UberWebhookPayload, restaurante_id: str) -> None:
@@ -70,7 +84,7 @@ class UberOrderUseCase:
 
     async def deny_order_in_uber(self, order_id: str, restaurante_id: str, reason: str, explanation: str) -> bool:
         # 1. Recuperamos el token
-        token = await self.token_cache.get_token(restaurante_id)
+        token = await self.token_cache.get_app_token(restaurante_id)
         if not token:
             raise ValueError("Token de Uber expirado o no encontrado.")
 
@@ -80,4 +94,33 @@ class UberOrderUseCase:
         print(f"❌ [NEGOCIO] Orden {order_id} rechazada en Uber. Razón: {explanation}")
         
         # (Futuro: Aquí actualizarás el estado interno a CANCELADA)
-        return True
\ No newline at end of file
+        return True
+
+
+    async def mark_order_ready_in_uber(
+        self,
+        order_id: str,
+        restaurante_id: str
+    ) -> bool:
+
+        # Para operaciones de pedidos Uber utilizamos
+        # Client Credentials + eats.order
+        token = await self.token_cache.get_app_token(restaurante_id)
+
+        if not token:
+            raise ValueError(
+                "App Token de Uber no encontrado o expirado. "
+                "Ejecute /app-token para generarlo."
+            )
+
+        await self.uber_api.mark_order_ready(
+            order_id,
+            token
+        )
+
+        print(
+            f"✅ [NEGOCIO] Orden {order_id} "
+            f"marcada como lista en Uber."
+        )
+
+        return True
diff --git a/kitchan-backend/src/kitchan/modules/integraciones/uber/application/webhook_use_cases.py b/kitchan-backend/src/kitchan/modules/integraciones/uber/application/webhook_use_cases.py
index 02aae9a..a7773fc 100644
--- a/kitchan-backend/src/kitchan/modules/integraciones/uber/application/webhook_use_cases.py
+++ b/kitchan-backend/src/kitchan/modules/integraciones/uber/application/webhook_use_cases.py
@@ -36,7 +36,15 @@ class UberWebhookUseCase:
         """
         # 1. Ignorar eventos que no nos interesan por ahora
         if payload.event_type != "orders.notification":
-            print(f"ℹ️ Ignorando evento de tipo: {payload.event_type}")
+            print("🚚 [UBER DELIVERY STATE]")
+            print(f"📦 ORDER ID: {payload.meta.order_id}")
+            print(f"🏪 STORE ID: {payload.meta.store_id}")
+            print(f"📍 STATUS: {payload.meta.status}")
+            print(payload)
+            return
+
+        if payload.event_type != "orders.notification":
+            print(f"ℹ️ [UBER] Evento ignorado: {payload.event_type}")
             return
 
         print(f"🛎️ [NEGOCIO] ¡Nueva orden detectada! ID Uber: {payload.meta.resource_id}")
diff --git a/kitchan-backend/src/kitchan/modules/integraciones/uber/domain/__pycache__/models.cpython-312.pyc b/kitchan-backend/src/kitchan/modules/integraciones/uber/domain/__pycache__/models.cpython-312.pyc
index 5e4eb10..df9c02f 100644
Binary files a/kitchan-backend/src/kitchan/modules/integraciones/uber/domain/__pycache__/models.cpython-312.pyc and b/kitchan-backend/src/kitchan/modules/integraciones/uber/domain/__pycache__/models.cpython-312.pyc differ
diff --git a/kitchan-backend/src/kitchan/modules/integraciones/uber/domain/__pycache__/ports.cpython-312.pyc b/kitchan-backend/src/kitchan/modules/integraciones/uber/domain/__pycache__/ports.cpython-312.pyc
index 06ccb7e..2c15720 100644
Binary files a/kitchan-backend/src/kitchan/modules/integraciones/uber/domain/__pycache__/ports.cpython-312.pyc and b/kitchan-backend/src/kitchan/modules/integraciones/uber/domain/__pycache__/ports.cpython-312.pyc differ
diff --git a/kitchan-backend/src/kitchan/modules/integraciones/uber/domain/models.py b/kitchan-backend/src/kitchan/modules/integraciones/uber/domain/models.py
index 9bb5314..345073c 100644
--- a/kitchan-backend/src/kitchan/modules/integraciones/uber/domain/models.py
+++ b/kitchan-backend/src/kitchan/modules/integraciones/uber/domain/models.py
@@ -2,10 +2,13 @@ from pydantic import BaseModel
 from typing import Optional
 
 class UberWebhookMeta(BaseModel):
-    """Metadatos del evento de Uber"""
-    resource_id: str
-    status: Optional[str] = None
-    user_id: Optional[str] = None
+    resource_id: Optional[str] = None 
+    status: Optional[str] = None 
+    user_id: Optional[str] = None 
+    order_id: Optional[str] = None 
+    courier_trip_id: Optional[str] = None 
+    external_order_id: Optional[str] = None 
+    store_id: Optional[str] = None
 
 class UberWebhookPayload(BaseModel):
     """Estructura esperada del Webhook de Uber Eats"""
diff --git a/kitchan-backend/src/kitchan/modules/integraciones/uber/domain/ports.py b/kitchan-backend/src/kitchan/modules/integraciones/uber/domain/ports.py
index a841bd7..860c41f 100644
--- a/kitchan-backend/src/kitchan/modules/integraciones/uber/domain/ports.py
+++ b/kitchan-backend/src/kitchan/modules/integraciones/uber/domain/ports.py
@@ -68,4 +68,15 @@ class UberApiPort(ABC):
 
     @abstractmethod
     async def deny_order(self, order_id: str, access_token: str, reason: str, explanation: str) -> bool:
+        pass
+
+    @abstractmethod 
+    async def cancel_order( self, order_id: str, access_token: str, reason: str, details: str | None = None ) -> bool: pass
+
+    @abstractmethod
+    async def mark_order_ready(
+        self,
+        order_id: str,
+        access_token: str
+    ) -> bool:
         pass
\ No newline at end of file
diff --git a/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/adapters/__pycache__/http_order_adapter.cpython-312.pyc b/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/adapters/__pycache__/http_order_adapter.cpython-312.pyc
index 0dcf049..fce1f39 100644
Binary files a/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/adapters/__pycache__/http_order_adapter.cpython-312.pyc and b/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/adapters/__pycache__/http_order_adapter.cpython-312.pyc differ
diff --git a/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/adapters/__pycache__/redis_token_adapter.cpython-312.pyc b/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/adapters/__pycache__/redis_token_adapter.cpython-312.pyc
index cf9c27d..6fa4b80 100644
Binary files a/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/adapters/__pycache__/redis_token_adapter.cpython-312.pyc and b/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/adapters/__pycache__/redis_token_adapter.cpython-312.pyc differ
diff --git a/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/adapters/http_order_adapter.py b/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/adapters/http_order_adapter.py
index cff9d98..6f33030 100644
--- a/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/adapters/http_order_adapter.py
+++ b/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/adapters/http_order_adapter.py
@@ -1,58 +1,380 @@
 import httpx
 from fastapi import HTTPException
+
 from src.kitchan.modules.integraciones.uber.domain.ports import UberApiPort
 
+
 class UberHttpAdapter(UberApiPort):
-    
-    async def get_order_details(self, order_id: str, access_token: str) -> dict:
-        
-        # --- 🧪 INTERCEPTOR DE PRUEBAS (Evita el 401 de Uber) ---
-        # --------------------------------------------------------
-
-        # Código real para producción
-        url = f"https://test-api.uber.com/v2/eats/order/{order_id}"
-        headers = {"Authorization": f"Bearer {access_token}", "Content-Type": "application/json"}
-        
+
+    BASE_URL = "https://test-api.uber.com"
+
+    # ============================================================
+    # OBTENER DETALLE DE ORDEN
+    # ============================================================
+
+    async def get_order_details(
+        self,
+        order_id: str,
+        access_token: str
+    ) -> dict:
+
+        url = f"{self.BASE_URL}/v1/delivery/order/{order_id}"
+
+        headers = {
+            "Authorization": f"Bearer {access_token}",
+            "Content-Type": "application/json",
+        }
+
+        print(f"📥 [UBER API] GET {url}")
+
         async with httpx.AsyncClient() as client:
-            respuesta = await client.get(url, headers=headers)
-            
-        if respuesta.status_code != 200:
-            raise HTTPException(status_code=respuesta.status_code, detail=f"Error en Uber: {respuesta.text}")
-            
-        return respuesta.json()
-
-    async def accept_order(self, order_id: str, access_token: str, reason: str = "Accepted") -> bool:
-        url = f"https://test-api.uber.com/v2/eats/order/{order_id}/accept_pos_order"
-        headers = {"Authorization": f"Bearer {access_token}", "Content-Type": "application/json"}
-        
+
+            response = await client.get(
+                url,
+                headers=headers
+            )
+
+        print(
+            f"📡 [UBER API] GET order -> "
+            f"{response.status_code}"
+        )
+
+        if response.status_code != 200:
+
+            print(
+                f"❌ [UBER API] Error obteniendo orden: "
+                f"{response.text}"
+            )
+
+            raise HTTPException(
+                status_code=response.status_code,
+                detail={
+                    "error": "UBER_GET_ORDER_FAILED",
+                    "mensaje": "No se pudo obtener la orden de Uber",
+                    "uber_response": response.text
+                }
+            )
+
+        return response.json()
+
+    # ============================================================
+    # ACEPTAR ORDEN
+    # ============================================================
+
+    async def accept_order(
+        self,
+        order_id: str,
+        access_token: str,
+        reason: str = "Accepted"
+    ) -> bool:
+
+        url = (
+            f"{self.BASE_URL}"
+            f"/v1/eats/orders/{order_id}/accept_pos_order"
+        )
+
+        headers = {
+            "Authorization": f"Bearer {access_token}",
+            "Content-Type": "application/json",
+        }
+
+        payload = {
+            "reason": reason
+        }
+
+        print(f"🚀 [UBER API] POST {url}")
+        print(f"📦 [UBER API] Payload: {payload}")
+
         async with httpx.AsyncClient() as client:
-            # Uber pide un JSON con el motivo (reason)
-            respuesta = await client.post(url, headers=headers, json={"reason": reason})
-            
-        if respuesta.status_code not in (200, 204):
-            print(f"❌ Error al aceptar orden {order_id}: {respuesta.text}")
-            raise HTTPException(status_code=respuesta.status_code, detail="No se pudo aceptar la orden en Uber")
-            
+
+            response = await client.post(
+                url,
+                headers=headers,
+                json=payload
+            )
+
+        print(
+            f"📡 [UBER API] ACCEPT ORDER -> "
+            f"{response.status_code}"
+        )
+
+        if response.status_code not in (200, 204):
+
+            print(
+                f"❌ [UBER API] Error aceptando orden "
+                f"{order_id}"
+            )
+
+            print(f"Status: {response.status_code}")
+            print(f"Response: {response.text}")
+
+            raise HTTPException(
+                status_code=response.status_code,
+                detail={
+                    "error": "UBER_ACCEPT_FAILED",
+                    "mensaje": "Uber rechazó la aceptación de la orden",
+                    "uber_status": response.status_code,
+                    "uber_response": response.text
+                }
+            )
+
+        print(
+            f"✅ [UBER API] Orden {order_id} aceptada correctamente"
+        )
+
         return True
 
-    async def deny_order(self, order_id: str, access_token: str, reason: str, explanation: str) -> bool:
-        url = f"https://test-api.uber.com/v2/eats/order/{order_id}/deny_pos_order"
-        headers = {"Authorization": f"Bearer {access_token}", "Content-Type": "application/json"}
-        
-        # Motivos válidos según Uber: ITEM_OUT_OF_STOCK, KITCHEN_CLOSED, OTHER
+
+    # ============================================================
+    # RECHAZAR ORDEN
+    # ============================================================
+
+    async def deny_order(
+        self,
+        order_id: str,
+        access_token: str,
+        reason: str,
+        explanation: str
+    ) -> bool:
+
+        url = (
+            f"{self.BASE_URL}"
+            f"/v1/eats/orders/{order_id}/deny_pos_order"
+        )
+
+        headers = {
+            "Authorization": f"Bearer {access_token}",
+            "Content-Type": "application/json",
+        }
+
         payload = {
             "reason": {
                 "explanation": explanation,
-                "out_of_item_details": {"item_ids": []} # Vacío si cancelamos todo
-            },
-            "reason_code": reason
+                "code": reason,
+                "out_of_stock_items": [],
+                "invalid_items": []
+            }
         }
-        
+
+        print(f"📤 [UBER API] POST {url}")
+        print(f"📦 Payload: {payload}")
+
         async with httpx.AsyncClient() as client:
-            respuesta = await client.post(url, headers=headers, json=payload)
-            
-        if respuesta.status_code not in (200, 204):
-            print(f"❌ Error al rechazar orden {order_id}: {respuesta.text}")
-            raise HTTPException(status_code=respuesta.status_code, detail="No se pudo rechazar la orden en Uber")
-            
-        return True
\ No newline at end of file
+
+            response = await client.post(
+                url,
+                headers=headers,
+                json=payload
+            )
+
+        print(
+            f"📡 [UBER API] DENY order -> "
+            f"{response.status_code}"
+        )
+
+        if response.status_code not in (200, 204):
+
+            print(
+                f"❌ [UBER API] Error rechazando orden "
+                f"{order_id}"
+            )
+
+            print(
+                f"Status: {response.status_code}"
+            )
+
+            print(
+                f"Response: {response.text}"
+            )
+
+            raise HTTPException(
+                status_code=response.status_code,
+                detail={
+                    "error": "UBER_DENY_FAILED",
+                    "mensaje": "Uber rechazó el rechazo de la orden",
+                    "uber_status": response.status_code,
+                    "uber_response": response.text
+                }
+            )
+
+        print(
+            f"✅ [UBER API] Orden {order_id} "
+            f"rechazada correctamente"
+        )
+
+        return True
+
+
+    # ============================================================
+    # CANCELAR ORDEN
+    # ============================================================
+
+    async def cancel_order(
+        self,
+        order_id: str,
+        access_token: str,
+        reason: str,
+        details: str | None = None
+    ) -> bool:
+
+        url = (
+            f"{self.BASE_URL}"
+            f"/v1/eats/orders/{order_id}/cancel"
+        )
+
+        headers = {
+            "Authorization": f"Bearer {access_token}",
+            "Content-Type": "application/json",
+        }
+
+        payload = {
+            "reason": reason
+        }
+
+        if details:
+            payload["details"] = details
+
+        print(f"📤 [UBER API] POST {url}")
+        print(f"📦 [UBER API] Payload: {payload}")
+
+        async with httpx.AsyncClient() as client:
+
+            response = await client.post(
+                url,
+                headers=headers,
+                json=payload
+            )
+
+        print(
+            f"📡 [UBER API] CANCEL ORDER -> "
+            f"{response.status_code}"
+        )
+
+        if response.status_code not in (200, 204):
+
+            print(
+                f"❌ [UBER API] Error cancelando orden "
+                f"{order_id}"
+            )
+
+            print(f"Status: {response.status_code}")
+            print(f"Response: {response.text}")
+
+            raise HTTPException(
+                status_code=response.status_code,
+                detail={
+                    "error": "UBER_CANCEL_FAILED",
+                    "mensaje": "Uber rechazó la cancelación de la orden",
+                    "uber_status": response.status_code,
+                    "uber_response": response.text
+                }
+            )
+
+        print(
+            f"✅ [UBER API] Orden {order_id} "
+            f"cancelada correctamente"
+        )
+
+        return True
+
+
+    # ============================================================
+    # MARCAR ORDEN COMO LISTA
+    # ============================================================
+
+    
+    
+    async def mark_order_ready(
+        self,
+        order_id: str,
+        access_token: str
+    ) -> bool:
+
+        url = (
+            f"{self.BASE_URL}"
+            f"/v1/delivery/order/{order_id}/ready"
+        )
+
+        headers = {
+            "Authorization": f"Bearer {access_token}",
+            "Content-Type": "application/json",
+        }
+
+        print(f"🚀 [UBER API] POST {url}")
+
+        async with httpx.AsyncClient(timeout=30) as client:
+            response = await client.post(
+                url,
+                headers=headers,
+                json={}
+            )
+
+        print(f"📡 URL REAL: {url}")
+        print(f"🔑 TOKEN: {access_token[:20]}...")
+        print(f"📦 RESPONSE BODY: {response.text}")
+        print(f"📋 RESPONSE HEADERS: {dict(response.headers)}")
+
+        print(
+            f"📡 [UBER API] MARK ORDER READY -> "
+            f"{response.status_code}"
+        )
+
+        if response.status_code != 200:
+            raise HTTPException(
+                status_code=response.status_code,
+                detail={
+                    "error": "UBER_MARK_READY_FAILED",
+                    "uber_status": response.status_code,
+                    "uber_response": response.text,
+                }
+            )
+
+        print(
+            f"✅ [UBER API] Orden {order_id} "
+            f"marcada como READY"
+        )
+
+        return True
+
+
+    async def get_order_status(
+        self,
+        order_id: str,
+        access_token: str
+    ) -> dict:
+
+        url = (
+            f"{self.BASE_URL}"
+            f"/v1/delivery/order/{order_id}"
+        )
+
+        headers = {
+            "Authorization": f"Bearer {access_token}",
+            "Content-Type": "application/json",
+        }
+
+        print(f"🔎 [UBER API] GET STATUS {url}")
+
+        async with httpx.AsyncClient() as client:
+            response = await client.get(
+                url,
+                headers=headers
+            )
+
+        print(
+            f"📡 [UBER API] GET STATUS -> "
+            f"{response.status_code}"
+        )
+
+        print(f"📦 [UBER API] RESPONSE -> {response.text}")
+
+        if response.status_code != 200:
+            raise HTTPException(
+                status_code=response.status_code,
+                detail={
+                    "error": "UBER_GET_ORDER_STATUS_FAILED",
+                    "uber_response": response.text
+                }
+            )
+
+        return response.json()
\ No newline at end of file
diff --git a/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/controllers/__pycache__/oauth_api.cpython-312.pyc b/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/controllers/__pycache__/oauth_api.cpython-312.pyc
index c9e5a08..0d61ead 100644
Binary files a/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/controllers/__pycache__/oauth_api.cpython-312.pyc and b/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/controllers/__pycache__/oauth_api.cpython-312.pyc differ
diff --git a/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/controllers/__pycache__/orders_api.cpython-312.pyc b/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/controllers/__pycache__/orders_api.cpython-312.pyc
index 6866204..45a10f8 100644
Binary files a/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/controllers/__pycache__/orders_api.cpython-312.pyc and b/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/controllers/__pycache__/orders_api.cpython-312.pyc differ
diff --git a/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/controllers/__pycache__/webhook_api.cpython-312.pyc b/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/controllers/__pycache__/webhook_api.cpython-312.pyc
index 3025158..35f924b 100644
Binary files a/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/controllers/__pycache__/webhook_api.cpython-312.pyc and b/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/controllers/__pycache__/webhook_api.cpython-312.pyc differ
diff --git a/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/controllers/oauth_api.py b/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/controllers/oauth_api.py
index 13ec437..ea72e7f 100644
--- a/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/controllers/oauth_api.py
+++ b/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/controllers/oauth_api.py
@@ -448,6 +448,15 @@ async def generate_app_token(
     access_token = tokens.get("access_token")
     expires_in = tokens.get("expires_in")
 
+    scope = tokens.get("scope")
+
+    print("============================================================")
+    print("🔐 TOKEN UBER GENERADO")
+    print(f"TYPE: {tokens.get('token_type')}")
+    print(f"SCOPE: {scope}")
+    print(f"EXPIRES: {expires_in}")
+    print("============================================================")
+
 
     if not access_token:
 
@@ -470,14 +479,12 @@ async def generate_app_token(
     )
 
 
-    return {
-        "mensaje": (
-            "Access Token de aplicación generado "
-            "y almacenado en Redis"
-        ),
-        "restaurante_id": restaurante_id,
-        "expira_en_segundos": expires_in
-    }
+    return{
+            "mensaje": "Access Token de aplicación generado y almacenado en Redis",
+            "restaurante_id": restaurante_id,
+            "expira_en_segundos": expires_in,
+            "scope": scope
+        }
 
 
 # ============================================================
@@ -684,10 +691,31 @@ async def provisionar_tienda_uber(
     url = f"{UBER_API_BASE}/v1/eats/stores/{data.store_id}/pos_data"
 
     # 3. Payload oficial para enlazar KITCHAN
+    
     payload = {
         "integrator_store_id": data.restaurante_id,
         "integrator_brand_id": "KITCHAN",
-        "store_configuration_data": f'{{"kitchan_restaurante_id": "{data.restaurante_id}"}}'
+
+        "is_order_manager": True,
+
+        "require_manual_acceptance": False,
+
+        "webhooks_config": {
+            "order_release_webhooks": {
+                "is_enabled": True
+            },
+            "schedule_order_webhooks": {
+                "is_enabled": True
+            },
+            "delivery_status_webhooks": {
+                "is_enabled": True
+            },
+            "webhooks_version": "1.0.0"
+        },
+
+        "store_configuration_data": (
+            f'{{"kitchan_restaurante_id": "{data.restaurante_id}"}}'
+        )
     }
 
     headers = {
diff --git a/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/controllers/orders_api.py b/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/controllers/orders_api.py
index d752af3..2b4ab98 100644
--- a/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/controllers/orders_api.py
+++ b/kitchan-backend/src/kitchan/modules/integraciones/uber/infrastructure/controllers/orders_api.py
@@ -1,14 +1,14 @@
 import os
 from fastapi import APIRouter, HTTPException, Query, Depends
 from pydantic import BaseModel
-
+import logging
 # Importamos nuestro Caso de Uso y los Adaptadores
 from src.kitchan.modules.integraciones.uber.application.order_use_cases import UberOrderUseCase
 from src.kitchan.modules.integraciones.uber.infrastructure.adapters.redis_token_adapter import RedisUberTokenAdapter
 from src.kitchan.modules.integraciones.uber.infrastructure.adapters.http_order_adapter import UberHttpAdapter
 from src.kitchan.modules.pedidos.application.crear_pedido_service import CrearPedidoUseCase
 from src.kitchan.modules.pedidos.infrastructure.adapters.integraciones_dispatcher import PedidosIntegracionesAdapter
-
+logger = logging.getLogger(__name__)
 
 router = APIRouter(
     prefix="/api/v1/integraciones/uber/orders",
@@ -51,9 +51,19 @@ async def accept_uber_order(
         return {"status": "success", "message": f"Pedido {order_id} aceptado en Uber Eats."}
     except ValueError as e:
         raise HTTPException(status_code=401, detail=str(e))
-    except Exception as e:
-        raise HTTPException(status_code=500, detail="Error interno al aceptar el pedido.")
+    except Exception as error:
+        logger.exception(
+                "❌ Error aceptando pedido Uber %s",
+                order_id
+        )
 
+        raise HTTPException(
+                status_code=500,
+                detail={
+                    "error": "UBER_ACCEPT_FAILED",
+                    "mensaje": str(error)
+                }
+            )
 @router.post("/{order_id}/deny")
 async def deny_uber_order(
     order_id: str,
@@ -75,4 +85,54 @@ async def deny_uber_order(
     except ValueError as e:
         raise HTTPException(status_code=401, detail=str(e))
     except Exception as e:
-        raise HTTPException(status_code=500, detail="Error interno al rechazar el pedido.")
\ No newline at end of file
+        raise HTTPException(status_code=500, detail="Error interno al rechazar el pedido.")
+
+@router.post("/{order_id}/ready")
+async def mark_uber_order_ready(
+    order_id: str,
+    restaurante_id: str = Query(
+        ...,
+        description="ID del restaurante en Kitchan"
+    ),
+    use_case: UberOrderUseCase = Depends(get_order_use_case)
+):
+    """
+    Marca una orden de Uber Eats como lista para recoger.
+    """
+
+    try:
+
+        await use_case.mark_order_ready_in_uber(
+            order_id=order_id,
+            restaurante_id=restaurante_id
+        )
+
+        return {
+            "status": "success",
+            "message": (
+                f"Pedido {order_id} "
+                "marcado como listo en Uber Eats."
+            )
+        }
+
+    except ValueError as e:
+
+        raise HTTPException(
+            status_code=401,
+            detail=str(e)
+        )
+
+    except Exception as error:
+
+        logger.exception(
+            "❌ Error marcando pedido Uber %s como listo",
+            order_id
+        )
+
+        raise HTTPException(
+            status_code=500,
+            detail={
+                "error": "UBER_MARK_READY_FAILED",
+                "mensaje": str(error)
+            }
+        )
\ No newline at end of file
diff --git a/kitchan-backend/src/kitchan/modules/pedidos/domain/__pycache__/entities.cpython-312.pyc b/kitchan-backend/src/kitchan/modules/pedidos/domain/__pycache__/entities.cpython-312.pyc
index fbab716..0fd4fd4 100644
Binary files a/kitchan-backend/src/kitchan/modules/pedidos/domain/__pycache__/entities.cpython-312.pyc and b/kitchan-backend/src/kitchan/modules/pedidos/domain/__pycache__/entities.cpython-312.pyc differ
diff --git a/kitchan-backend/src/kitchan/modules/pedidos/domain/__pycache__/ports.cpython-312.pyc b/kitchan-backend/src/kitchan/modules/pedidos/domain/__pycache__/ports.cpython-312.pyc
index 575b771..079b480 100644
Binary files a/kitchan-backend/src/kitchan/modules/pedidos/domain/__pycache__/ports.cpython-312.pyc and b/kitchan-backend/src/kitchan/modules/pedidos/domain/__pycache__/ports.cpython-312.pyc differ
diff --git a/kitchan-backend/src/kitchan/modules/pedidos/domain/ports.py b/kitchan-backend/src/kitchan/modules/pedidos/domain/ports.py
index 8ca4ecc..6c29290 100644
--- a/kitchan-backend/src/kitchan/modules/pedidos/domain/ports.py
+++ b/kitchan-backend/src/kitchan/modules/pedidos/domain/ports.py
@@ -16,4 +16,6 @@ class PedidoRepositoryPort(ABC):
         Retorna True si fue exitoso, False si el pedido no existe.
         """
         pass
+
+    
     
\ No newline at end of file
diff --git a/kitchan-backend/src/kitchan/modules/pedidos/infrastructure/adapters/__pycache__/integraciones_dispatcher.cpython-312.pyc b/kitchan-backend/src/kitchan/modules/pedidos/infrastructure/adapters/__pycache__/integraciones_dispatcher.cpython-312.pyc
index 4f4f2a1..7eeb826 100644
Binary files a/kitchan-backend/src/kitchan/modules/pedidos/infrastructure/adapters/__pycache__/integraciones_dispatcher.cpython-312.pyc and b/kitchan-backend/src/kitchan/modules/pedidos/infrastructure/adapters/__pycache__/integraciones_dispatcher.cpython-312.pyc differ
diff --git a/kitchan-backend/src/kitchan/modules/pedidos/infrastructure/adapters/__pycache__/memory_repository.cpython-312.pyc b/kitchan-backend/src/kitchan/modules/pedidos/infrastructure/adapters/__pycache__/memory_repository.cpython-312.pyc
index edbcc77..ad0c05b 100644
Binary files a/kitchan-backend/src/kitchan/modules/pedidos/infrastructure/adapters/__pycache__/memory_repository.cpython-312.pyc and b/kitchan-backend/src/kitchan/modules/pedidos/infrastructure/adapters/__pycache__/memory_repository.cpython-312.pyc differ
